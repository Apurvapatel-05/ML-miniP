# Screenshots

Add app screenshots here after running `streamlit run app/streamlit_app.py`:

- `home.png` — Home page
- `prediction.png` — Prediction page (with a result)
- `analytics.png` — Analytics dashboard

These are referenced by the main `README.md`.
